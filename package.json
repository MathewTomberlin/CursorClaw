{
  "name": "cursorclaw",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:security": "vitest run tests/security-tools.test.ts",
    "test:redteam": "vitest run tests/prompt-injection-redteam.test.ts",
    "test:bench": "vitest run tests/bench/",
    "security:audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "cron-parser": "^5.4.0",
    "fastify": "^5.6.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/ws": "^8.18.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
