# User (example)
# Copy to workspace root as USER.md. Main-session only; treat as private. Do not put secrets here.

Who is the human? Name, what to call them, timezone, pronouns (optional), notes.
Context: what they care about, projects, preferences. Build over time.
